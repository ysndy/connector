package com.example.connector.soohyun;

import androidx.fragment.app.ListFragment;

public class item_frag extends ListFragment {

    public item_frag() {

    }
/*
    @Override
    public View onCreateView(LayoutInflater inflater, ViewGroup container,
                             Bundle savedInstanceState) {
        FrameLayout layout = (FrameLayout) inflater.inflate(R.layout.fragment_item_frag, container, false);

   /* String[] list_item = {"상품명", "단가", "거래처"};

        // Inflate the layout for this fragment
       // FrameLayout layout = inflater.inflate(R.layout.fragment_item_frag, container, false);

    ListView listView = layout.findViewById(R.id.list_item);

    ArrayAdapter<String> arrayAdapter = new ArrayAdapter<String>(
            getActivity(),
            android.R.layout.simple_list_item_1,
            (List<String>) listView
    );
    listView.setAdapter(arrayAdapter);

    return layout;
    }

    }  */
}